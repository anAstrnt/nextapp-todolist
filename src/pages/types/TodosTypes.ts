export interface TodosTypes {
  todoDocId: string;
  todoId: string;
  todo: string;
  state: string;
  detail: string;
  deadline: string;
  timestamp: string;
  isTodoEditable: boolean;
  isDeadlineEditable: boolean;
  isDetailEditable: boolean;
}
